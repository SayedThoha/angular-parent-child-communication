# Angular Parent-Child Communication Example
This is a simple Angular application that demonstrates **parent-to-child** and **child-to-parent** communication using `@Input()` and `@Output()` decorators.
